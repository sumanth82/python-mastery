# Packages is a level above modules;
# modules are files and packages denotes a folder, with multiple files/modules within that.

# When you create a new package (folder) and modules within this package (.py files), it creates an __init__.py file within the package folder
# Intepreter then knows it's a python package
